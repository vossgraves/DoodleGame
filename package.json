{
  "name": "doodle-district",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A scribbled survival shooter — three.js + TypeScript remake (doodle shooter clone with zombies mode + mobile controls)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.3",
    "vite": "^6.4.3"
  }
}
